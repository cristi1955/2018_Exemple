@model IEnumerable<ckAzureDbApp.T1>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.k2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.k3)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.k2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.k3)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.k1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.k1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.k1 })
        </td>
    </tr>
}

</table>
